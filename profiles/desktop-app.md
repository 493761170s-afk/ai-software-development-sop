# Profile — Desktop Application

补充：OS/version matrix、installer/uninstaller、auto-update、filesystem permissions、native integrations、local data migration、crash recovery、code signing/notarization、offline operation、multi-window/IPC（适用）。

发布必须验证安装、升级、回滚/降级限制和用户数据不被意外破坏。
